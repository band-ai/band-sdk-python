# This file was auto-generated by Fern from our API Definition.

import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import jsonable_encoder
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..core.serialization import convert_and_respect_annotation_metadata
from ..errors.forbidden_error import ForbiddenError
from ..errors.not_found_error import NotFoundError
from ..errors.unauthorized_error import UnauthorizedError
from ..errors.unprocessable_entity_error import UnprocessableEntityError
from ..types.chat_message_request import ChatMessageRequest
from ..types.error import Error
from .types.create_agent_chat_message_response import CreateAgentChatMessageResponse
from .types.get_agent_next_message_response import GetAgentNextMessageResponse
from .types.list_agent_messages_request_status import ListAgentMessagesRequestStatus
from .types.list_agent_messages_response import ListAgentMessagesResponse
from .types.mark_agent_message_failed_response import MarkAgentMessageFailedResponse
from .types.mark_agent_message_processed_response import (
    MarkAgentMessageProcessedResponse,
)
from .types.mark_agent_message_processing_response import (
    MarkAgentMessageProcessingResponse,
)

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawAgentApiMessagesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list_agent_messages(
        self,
        chat_id: str,
        *,
        status: typing.Optional[ListAgentMessagesRequestStatus] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[ListAgentMessagesResponse]:
        """
        Returns messages that the agent needs to process, filtered by status.

        ## Default Behavior (no status param)

        Returns all messages that are NOT processed. This is the recommended way to get
        all work the agent should handle, including:
        - New messages (no delivery status yet)
        - Delivered messages (acknowledged but not started)
        - Processing messages (stuck/crashed - supports crash recovery)
        - Failed messages (available for retry)

        ## Status Filter Reference

        | ?status=     | Returns                                                              | Use Case                    |
        |--------------|----------------------------------------------------------------------|-----------------------------|
        | *(no param)* | Everything NOT processed                                             | Get all work to do          |
        | `pending`    | No status, delivered, or failed without active attempt               | Queue depth (untouched)     |
        | `processing` | Currently being processed                                            | In-flight work              |
        | `processed`  | Successfully completed                                               | Done items                  |
        | `failed`     | Failed only                                                          | Failure backlog             |
        | `all`        | All messages regardless of status                                    | Full history                |

        Messages are returned in chronological order (oldest first).

        ## Workflow

        After retrieving messages, you must update their processing status:

        1. `GET /messages` or `GET /messages/next` → Get work to do
        2. `POST /messages/{id}/processing` → **Required:** Mark as processing before you start
        3. Process the message (reasoning loop, tool calls, etc.)
        4. `POST /messages/{id}/processed` → Mark as done, OR
           `POST /messages/{id}/failed` → Mark as failed with error message
        5. Repeat

        **Important:** Always call `/processing` before starting work. This prevents
        duplicate processing since agents doing reasoning loops will always follow the
        sequence: `/next` → `/processing` → do work → `/processed`.

        ## Crash Recovery

        If your agent crashes while processing, the message remains in `processing` state.
        When the agent restarts:
        1. Call `GET /messages` (default) - it includes stuck `processing` messages
        2. The stuck message will be returned so you can retry it
        3. Call `/processing` again to reset the attempt timestamp, then continue

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        status : typing.Optional[ListAgentMessagesRequestStatus]
            Filter by processing status (default: all actionable messages)

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Items per page (default: 20, max: 100)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[ListAgentMessagesResponse]
            Messages
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages",
            method="GET",
            params={
                "status": status,
                "page": page,
                "page_size": page_size,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListAgentMessagesResponse,
                    parse_obj_as(
                        type_=ListAgentMessagesResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    def create_agent_chat_message(
        self,
        chat_id: str,
        *,
        message: ChatMessageRequest,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[CreateAgentChatMessageResponse]:
        """
        Creates a new text message in a chat room. The agent must be a participant in the room.

        This endpoint only supports `text` message type. For event-type messages
        (tool_call, tool_result, thought, system, error, action, guidelines, task),
        use `POST /agent/chats/{chat_id}/events` instead.

        Messages must include at least one @mention to ensure proper routing to recipients.

        Example request:
        ```json
        {
          "message": {
            "content": "@task.owner I have completed the analysis",
            "mentions": [
              {"id": "user-uuid", "handle": "task.owner", "name": "Task Owner"}
            ]
          }
        }
        ```

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        message : ChatMessageRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[CreateAgentChatMessageResponse]
            Message sent
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages",
            method="POST",
            json={
                "message": convert_and_respect_annotation_metadata(
                    object_=message, annotation=ChatMessageRequest, direction="write"
                ),
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateAgentChatMessageResponse,
                    parse_obj_as(
                        type_=CreateAgentChatMessageResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    def get_agent_next_message(
        self, chat_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[GetAgentNextMessageResponse]:
        """
        Returns the single oldest message that needs processing.

        ## What It Returns

        The oldest message that is NOT processed, including:
        - New messages (no delivery status yet)
        - Delivered messages (acknowledged but not started)
        - Processing messages (stuck/crashed - supports crash recovery)
        - Failed messages (available for retry)

        Returns **204 No Content** if there are no messages to process.

        ## Workflow

        This is the primary endpoint for agent reasoning loops:

        1. `GET /messages/next` → Get next work item
        2. `POST /messages/{id}/processing` → **Required:** Mark as processing
        3. Process the message (reasoning loop, tool calls, etc.)
        4. `POST /messages/{id}/processed` → Mark as done, OR
           `POST /messages/{id}/failed` → Mark as failed with error message
        5. Loop back to step 1

        ## Crash Recovery

        If your agent crashes while processing, the message stays in `processing` state.
        When restarted, calling `/next` will return that same stuck message (oldest first),
        allowing the agent to reclaim and retry it.

        ## Difference from GET /messages

        - `GET /messages` returns **all** actionable messages (for batch processing or queue inspection)
        - `GET /messages/next` returns **one** message (for sequential processing loops)

        Both use the same filter logic: everything that is NOT processed.

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[GetAgentNextMessageResponse]
            Next message
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/next",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    GetAgentNextMessageResponse,
                    parse_obj_as(
                        type_=GetAgentNextMessageResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    def mark_agent_message_processing(
        self,
        chat_id: str,
        id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[MarkAgentMessageProcessingResponse]:
        """
        Marks a message as being processed by the agent. This creates a new processing attempt
        with a system-managed timestamp. The agent must be a participant in the chat room.

        ## What It Does

        - Creates a new attempt with auto-incremented attempt_number
        - Sets the attempt status to "processing"
        - Records the started_at timestamp (system-managed)
        - Updates the agent's delivery status to "processing"

        ## Multiple Calls

        This endpoint can be called multiple times on the same message. Each call creates
        a **new attempt**. This is intentional for crash recovery:

        1. Agent calls `/processing` (attempt 1)
        2. Agent crashes while processing
        3. Agent restarts, calls `/next`, gets the same message back
        4. Agent calls `/processing` again (attempt 2)
        5. Agent completes processing, calls `/processed`

        The attempts array in the message metadata tracks the full history.

        ## Workflow

        Always call this endpoint before starting work on a message:

        1. `GET /messages/next` → Get message
        2. `POST /messages/{id}/processing` → **This endpoint**
        3. Process the message
        4. `POST /messages/{id}/processed` or `/failed`

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        id : str
            Message ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[MarkAgentMessageProcessingResponse]
            Message marked as processing
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/{jsonable_encoder(id)}/processing",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    MarkAgentMessageProcessingResponse,
                    parse_obj_as(
                        type_=MarkAgentMessageProcessingResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    def mark_agent_message_failed(
        self,
        chat_id: str,
        id: str,
        *,
        error: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[MarkAgentMessageFailedResponse]:
        """
        Marks a message processing as failed by the agent. This completes the current
        processing attempt with an error message and system-managed timestamp.

        ## What It Does

        - Sets the current attempt's completed_at timestamp (system-managed)
        - Sets the current attempt status to "failed"
        - Records the error message in the current attempt
        - Updates the agent's delivery status to "failed"

        ## Requirements

        **Requires an active processing attempt.** You must call `/processing` first.
        Returns 422 if no processing attempt exists.

        ## After Calling

        Failed messages remain available for retry. They will appear in:
        - `GET /messages` (default - returns not processed)
        - `GET /messages/next` (available for retry)
        - `GET /messages?status=failed`
        - `GET /messages?status=all`

        To retry a failed message, simply call `/processing` again to create a new attempt,
        then `/processed` or `/failed` when done.

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        id : str
            Message ID

        error : str
            Error message describing why processing failed

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[MarkAgentMessageFailedResponse]
            Message marked as failed
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/{jsonable_encoder(id)}/failed",
            method="POST",
            json={
                "error": error,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    MarkAgentMessageFailedResponse,
                    parse_obj_as(
                        type_=MarkAgentMessageFailedResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    def mark_agent_message_processed(
        self,
        chat_id: str,
        id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[MarkAgentMessageProcessedResponse]:
        """
        Marks a message as successfully processed by the agent. This completes the current
        processing attempt with a system-managed timestamp.

        ## What It Does

        - Sets the current attempt's completed_at timestamp (system-managed)
        - Sets the current attempt status to "success"
        - Sets the agent's processed_at timestamp (system-managed)
        - Updates the agent's delivery status to "processed"

        ## Requirements

        **Requires an active processing attempt.** You must call `/processing` first.
        Returns 422 if no processing attempt exists.

        ## After Calling

        Once marked as processed, the message will no longer appear in:
        - `GET /messages` (default - returns not processed)
        - `GET /messages/next`
        - `GET /messages?status=pending`

        It will only appear in:
        - `GET /messages?status=processed`
        - `GET /messages?status=all`

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        id : str
            Message ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[MarkAgentMessageProcessedResponse]
            Message marked as processed
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/{jsonable_encoder(id)}/processed",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    MarkAgentMessageProcessedResponse,
                    parse_obj_as(
                        type_=MarkAgentMessageProcessedResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )


class AsyncRawAgentApiMessagesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list_agent_messages(
        self,
        chat_id: str,
        *,
        status: typing.Optional[ListAgentMessagesRequestStatus] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[ListAgentMessagesResponse]:
        """
        Returns messages that the agent needs to process, filtered by status.

        ## Default Behavior (no status param)

        Returns all messages that are NOT processed. This is the recommended way to get
        all work the agent should handle, including:
        - New messages (no delivery status yet)
        - Delivered messages (acknowledged but not started)
        - Processing messages (stuck/crashed - supports crash recovery)
        - Failed messages (available for retry)

        ## Status Filter Reference

        | ?status=     | Returns                                                              | Use Case                    |
        |--------------|----------------------------------------------------------------------|-----------------------------|
        | *(no param)* | Everything NOT processed                                             | Get all work to do          |
        | `pending`    | No status, delivered, or failed without active attempt               | Queue depth (untouched)     |
        | `processing` | Currently being processed                                            | In-flight work              |
        | `processed`  | Successfully completed                                               | Done items                  |
        | `failed`     | Failed only                                                          | Failure backlog             |
        | `all`        | All messages regardless of status                                    | Full history                |

        Messages are returned in chronological order (oldest first).

        ## Workflow

        After retrieving messages, you must update their processing status:

        1. `GET /messages` or `GET /messages/next` → Get work to do
        2. `POST /messages/{id}/processing` → **Required:** Mark as processing before you start
        3. Process the message (reasoning loop, tool calls, etc.)
        4. `POST /messages/{id}/processed` → Mark as done, OR
           `POST /messages/{id}/failed` → Mark as failed with error message
        5. Repeat

        **Important:** Always call `/processing` before starting work. This prevents
        duplicate processing since agents doing reasoning loops will always follow the
        sequence: `/next` → `/processing` → do work → `/processed`.

        ## Crash Recovery

        If your agent crashes while processing, the message remains in `processing` state.
        When the agent restarts:
        1. Call `GET /messages` (default) - it includes stuck `processing` messages
        2. The stuck message will be returned so you can retry it
        3. Call `/processing` again to reset the attempt timestamp, then continue

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        status : typing.Optional[ListAgentMessagesRequestStatus]
            Filter by processing status (default: all actionable messages)

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Items per page (default: 20, max: 100)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[ListAgentMessagesResponse]
            Messages
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages",
            method="GET",
            params={
                "status": status,
                "page": page,
                "page_size": page_size,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListAgentMessagesResponse,
                    parse_obj_as(
                        type_=ListAgentMessagesResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    async def create_agent_chat_message(
        self,
        chat_id: str,
        *,
        message: ChatMessageRequest,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[CreateAgentChatMessageResponse]:
        """
        Creates a new text message in a chat room. The agent must be a participant in the room.

        This endpoint only supports `text` message type. For event-type messages
        (tool_call, tool_result, thought, system, error, action, guidelines, task),
        use `POST /agent/chats/{chat_id}/events` instead.

        Messages must include at least one @mention to ensure proper routing to recipients.

        Example request:
        ```json
        {
          "message": {
            "content": "@task.owner I have completed the analysis",
            "mentions": [
              {"id": "user-uuid", "handle": "task.owner", "name": "Task Owner"}
            ]
          }
        }
        ```

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        message : ChatMessageRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[CreateAgentChatMessageResponse]
            Message sent
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages",
            method="POST",
            json={
                "message": convert_and_respect_annotation_metadata(
                    object_=message, annotation=ChatMessageRequest, direction="write"
                ),
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateAgentChatMessageResponse,
                    parse_obj_as(
                        type_=CreateAgentChatMessageResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    async def get_agent_next_message(
        self, chat_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[GetAgentNextMessageResponse]:
        """
        Returns the single oldest message that needs processing.

        ## What It Returns

        The oldest message that is NOT processed, including:
        - New messages (no delivery status yet)
        - Delivered messages (acknowledged but not started)
        - Processing messages (stuck/crashed - supports crash recovery)
        - Failed messages (available for retry)

        Returns **204 No Content** if there are no messages to process.

        ## Workflow

        This is the primary endpoint for agent reasoning loops:

        1. `GET /messages/next` → Get next work item
        2. `POST /messages/{id}/processing` → **Required:** Mark as processing
        3. Process the message (reasoning loop, tool calls, etc.)
        4. `POST /messages/{id}/processed` → Mark as done, OR
           `POST /messages/{id}/failed` → Mark as failed with error message
        5. Loop back to step 1

        ## Crash Recovery

        If your agent crashes while processing, the message stays in `processing` state.
        When restarted, calling `/next` will return that same stuck message (oldest first),
        allowing the agent to reclaim and retry it.

        ## Difference from GET /messages

        - `GET /messages` returns **all** actionable messages (for batch processing or queue inspection)
        - `GET /messages/next` returns **one** message (for sequential processing loops)

        Both use the same filter logic: everything that is NOT processed.

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[GetAgentNextMessageResponse]
            Next message
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/next",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    GetAgentNextMessageResponse,
                    parse_obj_as(
                        type_=GetAgentNextMessageResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    async def mark_agent_message_processing(
        self,
        chat_id: str,
        id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[MarkAgentMessageProcessingResponse]:
        """
        Marks a message as being processed by the agent. This creates a new processing attempt
        with a system-managed timestamp. The agent must be a participant in the chat room.

        ## What It Does

        - Creates a new attempt with auto-incremented attempt_number
        - Sets the attempt status to "processing"
        - Records the started_at timestamp (system-managed)
        - Updates the agent's delivery status to "processing"

        ## Multiple Calls

        This endpoint can be called multiple times on the same message. Each call creates
        a **new attempt**. This is intentional for crash recovery:

        1. Agent calls `/processing` (attempt 1)
        2. Agent crashes while processing
        3. Agent restarts, calls `/next`, gets the same message back
        4. Agent calls `/processing` again (attempt 2)
        5. Agent completes processing, calls `/processed`

        The attempts array in the message metadata tracks the full history.

        ## Workflow

        Always call this endpoint before starting work on a message:

        1. `GET /messages/next` → Get message
        2. `POST /messages/{id}/processing` → **This endpoint**
        3. Process the message
        4. `POST /messages/{id}/processed` or `/failed`

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        id : str
            Message ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[MarkAgentMessageProcessingResponse]
            Message marked as processing
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/{jsonable_encoder(id)}/processing",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    MarkAgentMessageProcessingResponse,
                    parse_obj_as(
                        type_=MarkAgentMessageProcessingResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    async def mark_agent_message_failed(
        self,
        chat_id: str,
        id: str,
        *,
        error: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[MarkAgentMessageFailedResponse]:
        """
        Marks a message processing as failed by the agent. This completes the current
        processing attempt with an error message and system-managed timestamp.

        ## What It Does

        - Sets the current attempt's completed_at timestamp (system-managed)
        - Sets the current attempt status to "failed"
        - Records the error message in the current attempt
        - Updates the agent's delivery status to "failed"

        ## Requirements

        **Requires an active processing attempt.** You must call `/processing` first.
        Returns 422 if no processing attempt exists.

        ## After Calling

        Failed messages remain available for retry. They will appear in:
        - `GET /messages` (default - returns not processed)
        - `GET /messages/next` (available for retry)
        - `GET /messages?status=failed`
        - `GET /messages?status=all`

        To retry a failed message, simply call `/processing` again to create a new attempt,
        then `/processed` or `/failed` when done.

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        id : str
            Message ID

        error : str
            Error message describing why processing failed

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[MarkAgentMessageFailedResponse]
            Message marked as failed
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/{jsonable_encoder(id)}/failed",
            method="POST",
            json={
                "error": error,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    MarkAgentMessageFailedResponse,
                    parse_obj_as(
                        type_=MarkAgentMessageFailedResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )

    async def mark_agent_message_processed(
        self,
        chat_id: str,
        id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[MarkAgentMessageProcessedResponse]:
        """
        Marks a message as successfully processed by the agent. This completes the current
        processing attempt with a system-managed timestamp.

        ## What It Does

        - Sets the current attempt's completed_at timestamp (system-managed)
        - Sets the current attempt status to "success"
        - Sets the agent's processed_at timestamp (system-managed)
        - Updates the agent's delivery status to "processed"

        ## Requirements

        **Requires an active processing attempt.** You must call `/processing` first.
        Returns 422 if no processing attempt exists.

        ## After Calling

        Once marked as processed, the message will no longer appear in:
        - `GET /messages` (default - returns not processed)
        - `GET /messages/next`
        - `GET /messages?status=pending`

        It will only appear in:
        - `GET /messages?status=processed`
        - `GET /messages?status=all`

        Parameters
        ----------
        chat_id : str
            Chat Room ID

        id : str
            Message ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[MarkAgentMessageProcessedResponse]
            Message marked as processed
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/agent/chats/{jsonable_encoder(chat_id)}/messages/{jsonable_encoder(id)}/processed",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    MarkAgentMessageProcessedResponse,
                    parse_obj_as(
                        type_=MarkAgentMessageProcessedResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        Error,
                        parse_obj_as(
                            type_=Error,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 422:
                raise UnprocessableEntityError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(
                status_code=_response.status_code,
                headers=dict(_response.headers),
                body=_response.text,
            )
        raise ApiError(
            status_code=_response.status_code,
            headers=dict(_response.headers),
            body=_response_json,
        )
